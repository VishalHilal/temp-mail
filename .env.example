# Database Configuration
DATABASE_URL=postgres://postgres:postgres@localhost/tempmail

# SMTP Domain (the domain for your temporary email addresses)
SMTP_DOMAIN=tempmail.local

# Optional: Rust log level
RUST_LOG=info
